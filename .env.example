# .env로 복사해서 실제 값을 채우세요 (.env는 .gitignore에 있어 커밋되지 않음)

ANTHROPIC_API_KEY=

# OneDrive 동기화 밖의 경로 — 원본 영상·세션 데이터는 절대 이 리포 안(OneDrive 경로)에 두지 말 것
# (계획 "데이터 저장 위치" 섹션)
MSSB_DATA_DIR=C:\mssb_data

# OpenFace Windows 바이너리 설치 경로 (설치했다면)
OPENFACE_BIN_PATH=
