# 로컬에서 공개 사이트(src/public_app/app.py)를 테스트하려면 이 파일을
# .streamlit/secrets.toml로 복사하고 비밀번호를 채우세요 (.streamlit/secrets.toml은 .gitignore에 있음).
#
# 실제 배포(Streamlit Community Cloud) 시에는 이 파일 대신 앱 설정 화면의
# "Secrets" 탭에 아래 내용을 그대로 붙여넣으면 됩니다 — 저장소에 커밋하지 마세요.

APP_PASSWORD = "여기에_원하는_비밀번호"
